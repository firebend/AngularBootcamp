import { ClicksDirective } from './clicks.directive';

describe('ClicksDirective', () => {
  it('should create an instance', () => {
    const directive = new ClicksDirective();
    expect(directive).toBeTruthy();
  });
});
